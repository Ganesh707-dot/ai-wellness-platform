import SimplePage from "@/components/simple-page";

export default function ContactPage() {
  return (
    <SimplePage
      title="Contact"
      body="For demo access use the seeded accounts on the home page. For production deployments, configure RESEND_API_KEY for transactional email."
    />
  );
}
