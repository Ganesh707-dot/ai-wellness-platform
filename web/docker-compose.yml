services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: wellness
      POSTGRES_PASSWORD: wellness
      POSTGRES_DB: wellness_platform
    ports:
      - "5432:5432"
    volumes:
      - wellness_pg:/var/lib/postgresql/data

volumes:
  wellness_pg:
